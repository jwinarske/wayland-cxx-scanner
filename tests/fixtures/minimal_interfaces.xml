<?xml version="1.0" encoding="UTF-8"?>
<!-- Exercises every wl_interface-table construct the IME protocol family needs:
     client-created new_id (factory), object args referencing an internal and an
     external (wl_surface) interface, a nullable object, a destructor request,
     fd-in-request, a bitfield enum arg, a since-versioned request, an
     event-delivered new_id, and an array event arg. -->
<protocol name="iface_test">
  <interface name="wl_thing_manager" version="2">
    <request name="create_thing">
      <arg name="id" type="new_id" interface="wl_thing"/>
    </request>
    <request name="attach_surface">
      <arg name="thing" type="object" interface="wl_thing"/>
      <arg name="surface" type="object" interface="wl_surface" allow-null="true"/>
    </request>
    <request name="destroy" type="destructor"/>
  </interface>

  <interface name="wl_thing" version="2">
    <enum name="caps" bitfield="true">
      <entry name="none"  value="0"/>
      <entry name="a"     value="1"/>
      <entry name="b"     value="2"/>
    </enum>
    <request name="set_keymap">
      <arg name="fd" type="fd"/>
      <arg name="size" type="uint"/>
    </request>
    <request name="set_caps" since="2">
      <arg name="caps" type="uint" enum="caps"/>
    </request>
    <request name="set_title">
      <arg name="title" type="string"/>
    </request>
    <event name="spawn">
      <arg name="child" type="new_id" interface="wl_thing"/>
    </event>
    <event name="state">
      <arg name="payload" type="array"/>
      <arg name="label" type="string" allow-null="true"/>
    </event>
  </interface>
</protocol>
